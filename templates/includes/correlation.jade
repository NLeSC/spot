div.container-fluid.widget-inner
  div.container-fluid Correlate with 
    select(data-hook="filter-selector")
        option(selected="selected") Chose a filter
    | and color by
    select(data-hook="color-selector")
        option(selected="selected") Chose a filter
  div.container-fluid(data-hook="scatter-plot")
